#!/usr/bin/python3
for n in "Hola SoY Mayus":
    # print(f"Character: {n}, ASCII: {ord(n)}")
    if ord(n) >= 97 and ord(n) <= 122:
        n = ord(n) - 32
        print("{}".format(ord(n), end="")
    else:
        print("{}".format((n), end="")

